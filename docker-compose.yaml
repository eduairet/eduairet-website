services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - .:/app
    environment:
      - CI=true
    env_file:
      - .env
    command: sh -c "pnpm install && pnpm run dev"
